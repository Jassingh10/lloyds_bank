[pytest]
addopts = --cov=ingest_pipeline --cov-report=term-missing -v
markers =
    unit: marks tests as unit (fast, offline, mocked – runs in any environment)
    integration: marks tests as integration (requires GCP credentials & resources)
testpaths = tests
python_files = test_*.py